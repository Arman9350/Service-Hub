kind = "web"
previewPath = "/"
title = "ServiceHub"
version = "1.0.0"
id = "artifacts/servicehub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26115

[services.development]
run = "pnpm --filter @workspace/servicehub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/servicehub", "run", "build" ]
publicDir = "artifacts/servicehub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26115"
BASE_PATH = "/"
